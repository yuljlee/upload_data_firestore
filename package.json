{
  "name": "upload_data_firestore",
  "version": "1.0.0",
  "description": "Upload Data to Firebase Firestore Cloud Database",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "chris",
  "license": "ISC",
  "dependencies": {
    "firebase-admin": "^9.1.1"
  }
}
